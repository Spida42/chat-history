# Day 1 完整对话内容大纲（未精简版索引）

> 完整对话内容见：day1.md

---

## 1. LIBERO 仓库整体结构讲解

- 项目是干什么的（机器人终身学习基准平台，130 个任务）
- 一级目录完整结构图（`.gitignore`、`LICENSE`、`README.md`、`setup.py` 等）
- 各文件夹详解：
  - `benchmark_scripts/` — 每个脚本的用途表格
  - `images/` — 图片资源
  - `notebooks/` — 4 个 Notebook 的用途表格
  - `scripts/` — 10 个脚本的用途表格
  - `templates/` — 任务模板和场景 XML
- 核心代码 `libero/` 的三层子目录展开：
  - `libero/configs/` — Hydra 配置体系（data、policy、lifelong、train、eval）
  - `libero/libero/` — 仿真环境（envs、bddl_files、benchmark、robots、arenas）
  - `libero/lifelong/` — 算法引擎（algos、models、datasets、metric）
- BDDL 文件格式说明
- 新手学习路线建议（6 步）

## 2. 如何理解和使用 `libero/` 核心文件夹

- GitHub 项目的两种用法：当工具包调用 vs 当脚手���修改
- "厨房"类比：灶台=环境、菜谱=任务定义、锅铲=算法、食材=数据集
- `setup.py` 的 `pip install -e .` 机制解释
- 三个子文件夹的具体使用方式：
  - `libero/libero/` — 直接 import 调用，附代码示例
  - `libero/configs/` — 修改 YAML 参数，附命令行示例
  - `libero/lifelong/` — 两种用法（直接调用 vs 继承修改），附 EWC 源码和自定义算法代码示例
- `scripts/` 与 `libero/` 的区别（库 vs 命令行工具）
- 全局关系图（import 依赖图）
- 三种使用模式总结表（当库调用、当模板继承、改配置、直接运行）

## 3. 纠正误解：`create_dataset.py` 不是用来"看效果"的

- `create_dataset.py` 的真正用途（人工演示录制 → 标准数据集）
- `collect_demonstration.py` 的源码和功能
- `create_dataset.py` 的源码和功能
- 改完算法后的正确工作流（5 步流程图）
- `main.py` 中 `learn_one_task` 的评估源码（打印成功率的逻辑）
- `scripts/` 各脚本使用场景总结表

## 4. 四周学习计划

- 第 1 周：搭环境 + 跑通（安装命令、Notebook、首次训练命令）
- 第 2 周：精读训练流程（main.py 流程图、base.py 四个核心方法、configs、datasets.py）
- 第 3 周：精读已有算法（EWC vs ER 对比、对比实验命令）
- 第 4 周：动手写算法（新建文件、注册算法、创建配置、运行命令）
- 优先级总结表（精读 / 认真读 / 浏览 / 跳过）

## 5. 第 1 周聚焦：数据集取舍 + Notebook 入门

- 数据集下载选项（4 个子集的大小、内容、下载建议）
- 结论：只下 `libero_spatial`（约 3GB）
- Notebook 是什么（给完全没接触过的新手的解释）
- `quick_walkthrough.ipynb` 的 6 个 section 逐一讲解（重点 2.4 和 2.6）
- 数据结构详解（actions、obs 各维度的 shape 和含义）
- 怎么打开和运行 Notebook（命令 + VSCode 方式）
- 第 1 周修订后的每日操作清单

